import Verso
import VersoManual
import VersoBlueprint
import LeanOA.Ultraweak.SpectralProjection

open Verso.Genre
open Verso.Genre.Manual
open Informal

#doc (Manual) "Lower spectral projections" =>

This chapter begins Sakai's Section 1.11.  It develops the lower spectral
projections of a self-adjoint element through the endpoint part of Theorem
1.11.3: continuity from below, the two-sided estimate for spectral-band
increments, and convergence to zero and one at the two ends of the real line.

{index}[spectral projection]
{index}[spectral resolution]
{index}[continuous functional calculus]

For a self-adjoint element $`h` of a W-star algebra, Sakai writes
$`e(\lambda)=s((\lambda 1-h)^+)`.  Pointwise, the scalar function
$`x\mapsto(\lambda-x)^+` is supported on $`(-\infty,\lambda)`, so the public
Lean name records the interval as `spectralProjectionIio`.

This construction does not duplicate the continuous-functional-calculus
projection associated with a *clopen* spectral subset.  A half-line cut need
not be clopen on the spectrum, and its indicator need not be continuous.
Instead, the W-star support operation turns the continuous positive cutoff
into the required projection.  The broader name `spectralProjection` remains
available for a future set-indexed spectral-measure interface.

:::group "lower-spectral-projections"
Positive scalar cutoffs and their support projections.
:::

# Positive cutoffs and lower spectral projections
%%%
tag := "lower-spectral-projections"
%%%

:::definition "def:spectral_positive_part" (parent := "lower-spectral-projections") (lean := "CStarAlgebra.spectralPositivePart, CStarAlgebra.spectralPositivePart_eq_posPart, CStarAlgebra.spectralPositivePart_nonneg, CStarAlgebra.continuous_spectralPositivePart") (uses := "cor:spec_sa_homeo_spec_cstar")
Let $`A` be a unital C-star algebra and let $`h=h^*\in A`.  For
$`\lambda\in\mathbb R`, define
$`b_h(\lambda)=\operatorname{cfc}_h(x\mapsto(\lambda-x)^+)`.  Then
$`b_h(\lambda)=(\lambda 1-h)^+`, it is nonnegative, and the map
$`\lambda\mapsto b_h(\lambda)` is norm-continuous.

This layer needs no predual or W-star structure.  Continuity is asserted only
along scalar translates inside the commutative C-star algebra generated by
$`h`; it is not an operator-Lipschitz claim for positive part on arbitrary
noncommuting elements.
:::

:::proof "def:spectral_positive_part"
Composition in the continuous functional calculus identifies the scalar
function $`x\mapsto(\lambda-x)^+` with the positive part of
$`\lambda 1-h`.  The scalar positive-part map is $`1`-Lipschitz, so changing
$`\lambda` changes these functions uniformly on $`\sigma(h)`.  Mathlib's
fixed-element continuity theorem for the continuous functional calculus then
gives norm continuity.
:::

:::definition "def:lower_spectral_projection_Sak_1_11" (parent := "lower-spectral-projections") (lean := "WStarAlgebra.spectralProjectionIio, WStarAlgebra.spectralProjectionIio_eq_support_posPart, WStarAlgebra.spectralProjectionIio_mul_spectralPositivePart, WStarAlgebra.spectralPositivePart_mul_spectralProjectionIio, WStarAlgebra.spectralProjectionIio_mul_posPart, WStarAlgebra.posPart_mul_spectralProjectionIio, WStarAlgebra.spectralProjectionIio_le_iff") (uses := "def:spectral_positive_part, prop:left_right_support_characterization")
Let $`M` be a W-star algebra and $`h=h^*\in M`.  The lower spectral projection
at $`\lambda` is
$`e_h(\lambda)=s(b_h(\lambda))=s((\lambda 1-h)^+)`.

It is characterized without retaining a choice of predual: for every
projection $`p`,
$`e_h(\lambda)\leq p` if and only if
$`p b_h(\lambda)=b_h(\lambda)`.
Moreover, $`e_h(\lambda)` fixes $`b_h(\lambda)` on both the left and the
right; these identities are exposed as simplification lemmas for subsequent
spectral-resolution calculations.
:::

:::proof "def:lower_spectral_projection_Sak_1_11"
Apply the existing support construction to the bundled self-adjoint positive
element $`b_h(\lambda)`.  Its leastness statement is exactly the established
left-support universal property.
:::

:::proposition "prop:lower_spectral_projection_mono_Sak_1_11" (parent := "lower-spectral-projections") (lean := "WStarAlgebra.spectralProjectionIio_mono") (uses := "def:lower_spectral_projection_Sak_1_11, lem:support_star_compatibility")
If $`\lambda\leq\mu`, then
$`e_h(\lambda)\leq e_h(\mu)`.
:::

:::proof "prop:lower_spectral_projection_mono_Sak_1_11"
The pointwise inequality
$`(\lambda-x)^+\leq(\mu-x)^+` passes through Mathlib's order-preserving
continuous functional calculus.  The resulting positive elements are ordered,
so monotonicity of support projections gives the conclusion.
:::

:::lemma_ "lem:lower_spectral_projection_continuity_Sak_1_11_1" (parent := "lower-spectral-projections") (lean := "WStarAlgebra.isLUB_range_spectralProjectionIio_of_tendsto, WStarAlgebra.tendsto_spectralProjectionIio_of_monotone") (uses := "def:spectral_positive_part, prop:lower_spectral_projection_mono_Sak_1_11, prop:proj_compl_lat_wstar_Sak_1_10_2")
Let $`I` be a nonempty directed preorder and let
$`\lambda_i` be an increasing net of real numbers converging to $`\lambda`.
Then $`e_h(\lambda)` is the least upper bound of the projections
$`e_h(\lambda_i)`, and
$`e_h(\lambda_i)\to e_h(\lambda)` ultraweakly.

Taking $`I=\mathbb N` gives Sakai's Lemma 1.11.1 for every increasing
sequence $`\lambda_n<\lambda` with $`\lambda_n\to\lambda`; the strict
inequality is not needed by the formal theorem.
:::

:::proof "lem:lower_spectral_projection_continuity_Sak_1_11_1"
Norm continuity of $`b_h` shows that any projection fixing every
$`b_h(\lambda_i)` also fixes $`b_h(\lambda)`.  The support universal property
therefore identifies $`e_h(\lambda)` as the least upper bound of the net of
projections.  Reuse the existing theorem that a directed projection supremum
is also an ambient order supremum, then apply monotone convergence in the
specified ultraweak topology.  No spectral measure or Radon--Stieltjes
integration is introduced at this checkpoint.
:::

:::group "spectral-band-increments"
Cut recovery, commutation, and the increment estimate of Sakai 1.11.2.
:::

# Spectral-band increments
%%%
tag := "spectral-band-increments"
%%%

:::proposition "prop:lower_spectral_projection_cut_recovery" (parent := "spectral-band-increments") (lean := "WStarAlgebra.sub_mul_spectralProjectionIio, WStarAlgebra.spectralProjectionIio_mul_sub, WStarAlgebra.commute_sub_spectralProjectionIio, WStarAlgebra.commute_spectralProjectionIio, WStarAlgebra.smul_spectralProjectionIio_sub_spectralPositivePart") (uses := "def:lower_spectral_projection_Sak_1_11, prop:left_right_support_characterization")
For every real $`\lambda`, the defining cutoff is recovered on either side
of its support:
$`
(\lambda 1-h)e_h(\lambda)
=e_h(\lambda)(\lambda 1-h)
=b_h(\lambda).
`
In particular, $`h` commutes with $`e_h(\lambda)` and
$`
\lambda e_h(\lambda)-b_h(\lambda)=h e_h(\lambda).
`
:::

:::proof "prop:lower_spectral_projection_cut_recovery"
Write $`x=\lambda 1-h=x^+-x^-`.  The support-zero-kernel
characterization and Mathlib's identities $`x^-x^+=x^+x^-=0` show that
$`x^-` is killed by $`s(x^+)` on both sides.  The two recovery identities
follow.  Their equality gives commutation with $`x`, hence with $`h` after
subtracting the central scalar $`\lambda 1`.  This proof uses no chosen
predual.
:::

:::lemma_ "lem:lower_spectral_projection_increment_bounds_Sak_1_11_2" (parent := "spectral-band-increments") (lean := "WStarAlgebra.spectralProjectionIio_band_bounds, WStarAlgebra.spectralProjectionIio_increment_bounds") (uses := "prop:lower_spectral_projection_mono_Sak_1_11, prop:lower_spectral_projection_cut_recovery")
If $`\lambda\leq\mu` and
$`p=e_h(\mu)-e_h(\lambda)`, then $`p` is a projection and
$`
\lambda p\leq hp\leq\mu p.
`
Consequently,
$`
\lambda\bigl(e_h(\mu)-e_h(\lambda)\bigr)
\leq
\bigl(\mu e_h(\mu)-b_h(\mu)\bigr)
-\bigl(\lambda e_h(\lambda)-b_h(\lambda)\bigr)
\leq
\mu\bigl(e_h(\mu)-e_h(\lambda)\bigr).
`
This is Sakai's Lemma 1.11.2.  The formal result strengthens the printed
strict hypothesis $`\lambda<\mu` to $`\lambda\leq\mu`.
:::

:::proof "lem:lower_spectral_projection_increment_bounds_Sak_1_11_2"
Monotonicity makes $`p=e_h(\mu)-e_h(\lambda)` a projection.  On this band,
the positive part of $`\lambda 1-h` and the negative part of
$`\mu 1-h` vanish.  The remaining products are nonnegative because the
functional-calculus parts commute with $`p`.  This proves
$`\lambda p\leq hp\leq\mu p`.  Replacing each
$`t e_h(t)-b_h(t)` by $`h e_h(t)` gives the displayed increment estimate.
:::

:::group "spectral-family-endpoints"
The endpoint behavior in Sakai 1.11.3.
:::

# Endpoint behavior
%%%
tag := "spectral-family-endpoints"
%%%

:::proposition "prop:lower_spectral_projection_endpoints_Sak_1_11_3" (parent := "spectral-family-endpoints") (lean := "WStarAlgebra.spectralProjectionIio_eq_zero_of_le_neg_norm, WStarAlgebra.spectralProjectionIio_eq_one_of_norm_lt, WStarAlgebra.tendsto_spectralProjectionIio_atBot, WStarAlgebra.tendsto_spectralProjectionIio_atTop") (uses := "def:lower_spectral_projection_Sak_1_11, lem:support_star_compatibility")
For a self-adjoint $`h` in a W-star algebra,
$`
  e_h(\lambda)=0 \quad\text{if }\lambda\leq-\lVert h\rVert,
  \qquad
  e_h(\lambda)=1 \quad\text{if }\lVert h\rVert<\lambda.
`
Consequently, for any specified predual,
$`e_h(\lambda)` converges ultraweakly to $`0` as $`\lambda\to-\infty`
and to $`1` as $`\lambda\to+\infty`.

The lower bound is stated with a weak inequality, strengthening the strict
version used in Sakai's proof.  The strict upper inequality is essential in
general: at $`\lambda=\lVert h\rVert`, a top spectral value can remain in the
kernel of $`\lambda1-h`.
:::

:::proof "prop:lower_spectral_projection_endpoints_Sak_1_11_3"
Mathlib's order bounds for a self-adjoint element give
$`-\lVert h\rVert1\leq h\leq\lVert h\rVert1`.  Below the lower endpoint,
$`\lambda1-h\leq0`, so its positive part and hence its support vanish.
Above the upper endpoint, $`\lambda1-h` is bounded below by the positive
scalar $`(\lambda-\lVert h\rVert)1`.  The general support-one criterion then
shows that its positive part has full support.  Both nets are eventually
constant, which gives the ultraweak limits without invoking a convergence
theorem specific to projections.
:::
