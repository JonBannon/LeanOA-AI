# Shared API status

Status reflects integration review through both separately formalized clauses of Sakai
Proposition 1.15.2
(2026-09-02).
The Section 1.14 sequence began at `05c69abaa5a8608700a75d25b4da05d04d63a588`. This is not a
promise of permanent immutability.

## GREEN — stable downstream surfaces

| Area | Principal modules | Why stable / consumers |
| --- | --- | --- |
| Weak-duality and specified-predual bridges | `IsWeak`, `Ultraweak.Basic`, `Ultraweak.Dual`, `Ultraweak.WStarAlgebra` | Named maps and compatibility interfaces underpin most ultraweak work. Changes require deliberate migration, but current clients may depend on them. |
| Ultraweak and intrinsic strong topology | `Ultraweak.Algebra`, `ContinuousStar`, `Multiplication`, `Strong`, `StrongProjection` | Established topology-facing API. Projection domination plus ultraweak convergence now has a reusable strong upgrade, and directed projection LUBs converge strongly. `Strong.isClosed_iff_image_toUltraweakEquiv` packages the existing real-convex closure comparison as an intrinsic closedness equivalence; it does not identify this topology with a concrete operator topology. |
| Concrete WOT/SOT comparison | `Mathlib.Analysis.LocallyConvex.WeakOperatorTopology` | `PointwiseConvergenceCLM.toWOT` is the general continuous identity from Mathlib's pointwise-convergence/SOT synonym to its WOT synonym, and `PointwiseConvergenceCLM.isClosed_pointwise_of_isClosed_wot` gives the corresponding closed-set implication. These are theorem-level bridges over Mathlib's existing types, not new topology definitions. |
| Vector-functional description of WOT | `Mathlib.Analysis.InnerProductSpace.WeakOperatorTopology`, `WeakOperatorTopology` | `vectorFunctional` and its algebraic span are generic over `RCLike`; raw/span separation, intrinsic-star stability, and fixed-multiplier invariance are proved. `vectorFunctionalWeakEquiv` is the bidirectional identity equivalence with Mathlib WOT, while `vectorFunctionalPairing_isWeak` and `vectorFunctionalSpanEquivDual` certify `σ(B(E,F),V) = WOT` and identify `V` with every WOT-continuous functional. This is not a norm completion or predual instance. |
| Vector-functional predual of bounded maps | `Mathlib.Analysis.InnerProductSpace.OperatorPredual`, `Ultraweak.BoundedOperator` | The coefficient span's actual norm closure is dense over its finite core, and canonical evaluation is an isometric equivalence onto its dual for maps from a seminormed space into a complete Hilbert space. `VectorFunctionalPredual` is only a short assembly carrier canonically isometric to that closure; it makes the existing `Predual` class usable at the naturally normed-domain boundary without a trace-class synonym or competing predual semantics. |
| Square-summable coefficient-series bridge | `Mathlib.Analysis.InnerProductSpace.OperatorPredualSeries`, `Ultraweak.BoundedOperatorCoefficientSeries`, `Ultraweak.BoundedOperatorInvariantTestSpace`, `Ultraweak.TestWeak` | Separate square summability gives norm convergence, evaluation, and the sharp Cauchy--Schwarz bound at arbitrary-index `RCLike` generality. The source test space uses exactly countable series, contains the invariant dense finite core, and receives a continuous identity from the full concrete-predual topology. This is a one-sided comparison, not a topology equality. |
| Ambient-relative Kaplansky density and WOT closure | `CStarAlgebra.KaplanskyDensity`, `Ultraweak.KaplanskyDensity`, `Ultraweak.RelativeKaplanskyDensity`, `Ultraweak.BoundedOperatorWOTClosure`, `Ultraweak.BoundedOperatorRelativeKaplansky` | `testWeakClosure` is a transported set closure on the original algebra carrier. The generic theorem accepts an explicit target star subalgebra equal to that closure; it does not invent another algebra or predual. The finite coefficient core identifies this target with Mathlib's WOT closure on `B(H)`, yielding ultraweak and Mackey unit-ball density. The geometric extreme-point transfer is proved at arbitrary real-submodule generality. |
| Concrete ultrastrong topology and intrinsic-strong comparison | `Mathlib.Analysis.LocallyConvex.SquareSummableConvergenceCLM`, `Ultraweak.BoundedOperatorUltrastrong`, `Ultraweak.BoundedOperatorStrongTopology` | The generic carrier is generated by the square-summable application seminorms and maps continuously to Mathlib pointwise/SOT. On `B(H)`, each defining seminorm is exactly the GNS seminorm of a positive diagonal coefficient-series functional. Filter-general positive-square criteria now give the converse comparisons on norm-closed balls used by Sakai 1.15.2(2); no global converse or topology equality is claimed. |
| Bounded-operator closedness equivalence | `Ultraweak.TestWeak`, `Ultraweak.Strong`, `Ultraweak.BoundedOperatorClosedness` | `operatorTopologyClosedness_tfae` proves Sakai Proposition 1.15.1 for a possibly nonunital self-adjoint subalgebra of `B(H)`. The predicates retain the five distinct source topologies, and the reverse WOT implication uses relative Kaplansky density plus scalar rescaling. The generic test-space restriction and real-convex strong/ultraweak closedness transports are reusable independently. No later topology equality is claimed. |
| Induced ultraweak subspaces and bounded weak-family topology | `Ultraweak.Dual`, `Ultraweak.NonUnitalStarSubalgebra`, `Ultraweak.BoundedOperatorTopology` | The quotient-predual topology of an ultraweakly closed submodule is canonically the ambient ultraweak subspace topology at `RCLike` generality. For an ultraweakly closed self-adjoint subalgebra of `B(H)`—equivalently WOT closed here—canonical homeomorphisms and arbitrary-filter corollaries identify intrinsic `sigma(N,N_*)`, coefficient-series sigma-WOT, and WOT on every zero-centered norm-closed ball. This is Sakai Proposition 1.15.2(1), not a global topology equality. |
| Induced strong subalgebras and bounded strong-family topology | `Ultraweak.InducedStrong`, `Ultraweak.BoundedOperatorStrongTopology` | `InducedStrong` is the quotient-predual intrinsic strong topology for any ultraweakly closed self-adjoint subalgebra of a dual $C^*$-algebra. Generic SOT and ultrastrong closed-ball carriers are available over normed spaces and scalars. In the source specialization, canonical homeomorphisms and arbitrary-filter corollaries identify intrinsic strong, coefficient-series ultrastrong, and SOT on every zero-centered norm-closed ball. This source-formalizes Proposition 1.15.2(2), but asserts no global topology equality. |
| Fixed-projection ultraweak decomposition | `Ultraweak.ProjectionDecomposition` | General ordered-C-star theorem layer: a fixed extraction identity separates an ultraweak difference limit and identifies positive/negative parts under eventual positivity. It introduces no spectral-family abstraction. |
| Projection lattice and support | `ProjectionLattice`, `Support`, `Corner` | Completed Section 1.10 and current spectral construction consume these interfaces. Support leastness, projection simp lemmas, and the positive-scalar projection lower-bound criterion are stable consumers of the same support object. |
| Source normality characterizations | `Ultraweak.NormalOrder` | `IsNormalOnProjections` remains canonical and is equivalent to full Scott continuity, preservation of projection-chain LUBs, Sakai's bounded directed-positive condition, and specified-predual membership. No new predicate or chosen-predual leak was introduced. |
| Arbitrary orthogonal projection sums | `Ultraweak.OrthogonalProjectionSum` | `Finset` partial sums, the existing projection `iSup`, ambient LUB, and ultraweak/strong convergence form a theorem-only arbitrary-index API with no new projection family or infinite-sum object. |
| Projection-chain decomposition and complete additivity | `Ultraweak.ProjectionChain`, `Ultraweak.CompleteAdditivity` | Every nonempty projection chain with a LUB admits an arbitrary orthogonal family whose finite sums are dominated by chain members and whose supremum is the chain LUB. Positive-functional normality is equivalent to arbitrary-family complete additivity, expressed by `HasSum`; there is no bundled predicate, countability hypothesis, or second normality notion. |
| Normal-positive-functional support | `CStarAlgebra.PositiveLinearFunctional`, `Mathlib.RingTheory.Idempotents`, `Ultraweak.FunctionalSupport` | The reusable null left ideal is defined at unital $C^*$-algebra generality. Explicit projection normality gives source-order strong then ultraweak closedness; the existing ideal classifier supplies an intrinsic functional support with the chosen predual hidden. Annihilation, greatest-zero, cutdown, full-support, and existing-corner interfaces introduce no second element support, `Faithful` predicate, or corner structure. |
| Norm orthogonality and self-adjoint normal-functional Jordan decomposition | `CStarAlgebra.PositiveLinearFunctional`, `Ultraweak.FunctionalSupport`, `Ultraweak.FunctionalJordanDecomposition` | `PositiveLinearMap.IsOrthogonal` is Sakai's norm definition at general nonunital $C^*$-algebra level. `Ultraweak.existsUnique_orthogonal_decomposition_of_isSelfAdjoint` gives the exact unique normal positive pair on a $W^*$-algebra. Support-product zero is a proved characterization, not a competing definition; the proof reuses the existing self-adjoint-unitary factorization and publishes no choice-based parts or new decomposition structure. |
| General normal-functional polar decomposition | `CStarAlgebra.PositiveLinearFunctional`, `Ultraweak.NormalCharacterization`, `Ultraweak.FunctionalSupport`, `Ultraweak.FunctionalPolarDecomposition` | `Ultraweak.existsUnique_functional_polar_decomposition` gives Sakai 1.14.4 with the source right-action convention `g x = φ (x * v)`, norm equality, initial support, uniqueness, and final support through `Ultraweak.functionalAbs` of the adjoint. `PositiveLinearMap.conjugate` is the reusable nonunital $C^*$-level construction; normality and support transport are downstream. The API adds no partial-isometry predicate: `star v * v = support φ` supplies the established projection semantics. |
| Ideals and central support | `Annihilator`, `Ideal`, `TwoSidedIdeal`, `CentralSupport`, `Opposite` | Completed, documented native-object API. The alternative upstream representation remains a separate review question. |
| Lower spectral projections | `CStarAlgebra.Spectral`, `Ultraweak.SpectralProjection`, `Ultraweak.SpectralProjectionStrong` | Half-line semantics and naming are intentionally fixed. Sakai 1.11.1 is source-formalized in `s(M,M_*)` without an extra monotonicity hypothesis; current finite sums depend on the same projection object. |
| Finite spectral sums, bands, and convergence | `Ultraweak.SpectralSum`, `Ultraweak.SpectralApproximation`, `Ultraweak.SpectralBand`, `Ultraweak.TaggedSpectralSum`, `Ultraweak.TruncatedSpectralSum` | Checked theorem-level frontier, including arbitrary tagged sums, sharp truncated-affine mesh estimates with an unaligned cutoff, and explicit norm-to-ultraweak convergence to the existing CFC target. These modules deliberately commit to no spectral-measure representation. |
| Element polar decomposition | `Mathlib.Analysis.CStarAlgebra.Abs`, `Mathlib.Analysis.CStarAlgebra.Projection`, `Ultraweak.AbsSupport`, `Ultraweak.ElementPolarDecomposition` | The general nonunital $C^*$-annihilator and projection consequences, together with an abstract nonunital real-CFC modulus-square identity, feed the source-faithful $W^*$-algebra theorem. `WStarAlgebra.exists_element_polar_decomposition` preserves the existence-only interface; `WStarAlgebra.element_polar_decomposition_unique` isolates algebraic uniqueness under only the factorization and initial-support equations; `WStarAlgebra.existsUnique_element_polar_decomposition` exposes exact Theorem 1.12.1. The regularizer, chosen predual, cluster filter, and support-defect cutdown remain private. |
| Verso package architecture | `docs/SakAIDocs`, `scripts/build-verso-site.sh` | Sole documentation source with verified migration parity and checked declaration links. |

## YELLOW — evolving, consume cautiously

| Area | Owner | Reason / consumers | Stabilization criterion |
| --- | --- | --- | --- |
| Mirrored Mathlib extension layer | architecture + Mathlib reconnaissance | Some declarations may move upstream or be replaced by newer Mathlib APIs. | Per-module overlap audit and a migration plan for any upstream replacement. |
| Broader positive-functional / representation assembly | architecture | Functional support, orthogonal Jordan decomposition, and general functional polar decomposition are stable focused slices, but Sak-AI still has no separate broad representation layer beyond Mathlib GNS and these APIs. | A later Sakai consumer demonstrates another missing interface and design review chooses its home. |
| Sakai Proposition 1.15.2 exact package | lead architecture | Both topology-level, arbitrary-filter closed-ball clauses are source-formalized separately, but no single declaration yet preserves Sakai's exact two-clause source order. A direct SOT/ultrastrong comparison on arbitrary operator-norm-bounded sets is also a deferred generality opportunity, not a source blocker. | Package the two clauses in exact source order, without turning restricted homeomorphisms into global topology equality. |
| Legacy mathematical typography in Verso | Verso stream | Existing source consistently uses older plain-text forms; new prose follows the recorded convention. | One systematic documentation-only migration, not scattered edits. |

## RED — architecture owner only

| Area | Owner | Reason / downstream pressure | Stabilization criterion |
| --- | --- | --- | --- |
| Arbitrary lower spectral resolution and its relationship to a set-indexed projection-valued measure | lead architecture | A refinement-plus-mesh finite-cut candidate, prescribed-cut stability, and complete pointwise/family uniqueness are kernel-checked in scratch. The source audit proves that Sakai uses `s(M,M_*)`, not `σ(M,M_*)`, but classifies the undefined integral semantics LEVEL C. Strong convergence of the same candidate net is checked to imply the existing ultraweak hypotheses. | Do not publish the candidate as Sakai's semantics. A future PVM must induce the intrinsic laws and a precisely stated representation by lower-half-line restriction. |
| Operator-valued spectral integral | lead architecture | D002's candidates remain inadequate. The refinement transaction removes the technical cofinality, endpoint, mesh, and support blockers for a clarified modern theorem, while the historical term remains genuinely ambiguous. | Stabilize only when a genuine mathematical consumer or a coherent PVM construction fixes the interface; never label the checked candidate as Sakai's definition. |
| Alternative `TwoSidedIdeal` representation or instance refactor | human review + architecture | `REVIEW_QUEUE.md` records two materially different designs. | Human decision or upstream Mathlib resolution. |
| Foundational predual/representation instance redesign | human review + architecture | Junction API with broad downstream dependence and explicit anti-definitional-equality constraints. | A documented migration with compatibility proof and downstream validation. |
